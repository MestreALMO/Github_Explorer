import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  color: #fff;

  svg {
    padding: 0 10px 0 0;
    font-size: 60px;
  }

  strong {
    font-size: 28px;
    font-family: cursive;
  }
`;
